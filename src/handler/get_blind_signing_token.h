#pragma once

/**
 * Handler for GET_APP_CONFIG command that will return the state of the application settings
 *
 * @return zero or positive integer if success, negative integer otherwise.
 *
 */
int handler_get_app_configuration();
